import styled from "styled-components";

export const Wrapper = styled.div``;
export const Header = styled.ul`
  padding: 0;
  border-bottom: ${({ theme }) => `solid 1px ${theme.grey}`};
`;
export const Body = styled.div``;
